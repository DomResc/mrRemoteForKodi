<Page
    x:Class="mrRemoteForKodi.Views.ConfiguredRemotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="12,0,12,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="TextBlockTitle"
            x:Uid="ConfiguredRemotes_TextBlockTitle"
            Margin="0,0,12,7"
            VerticalAlignment="Center"
            FontSize="28"
            FontWeight="SemiLight"
            TextTrimming="CharacterEllipsis"
            TextWrapping="NoWrap" />

        <Grid Grid.Row="1" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

            <RelativePanel MaxWidth="250" Visibility="{x:Bind IsTextBlockEmptyVisible, Mode=OneWay}">
                <TextBlock
                    FontFamily="Segoe MDL2 Assets"
                    RelativePanel.Above="TextBlockEmptyList"
                    RelativePanel.AlignHorizontalCenterWithPanel="True"
                    Style="{ThemeResource TitleTextBlockStyle}"
                    Text="&#xE783;" />

                <TextBlock
                    x:Name="TextBlockEmptyList"
                    x:Uid="ConfiguredRemotes_TextBlockEmptyList"
                    Margin="0,8,0,0"
                    RelativePanel.AlignHorizontalCenterWithPanel="True"
                    RelativePanel.AlignVerticalCenterWithPanel="True"
                    Style="{ThemeResource BodyTextBlockStyle}" />
            </RelativePanel>

        </Grid>

        <RelativePanel Grid.Row="2" Margin="-12,0,-12,0">

            <CommandBar
                Background="Transparent"
                RelativePanel.AlignLeftWithPanel="True"
                RelativePanel.AlignRightWithPanel="True">

                <AppBarButton
                    x:Uid="ConfiguredRemotes_AppBarButtonAddARemote"
                    Click="AddARemoteButton_Click"
                    Icon="Add" />
            </CommandBar>

        </RelativePanel>

        <!--  Adaptive triggers  -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="TextBlockTitle.Margin" Value="48,0,12,7" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>